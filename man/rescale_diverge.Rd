% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rescale.R
\name{rescale_diverge}
\alias{rescale_diverge}
\title{Rescale diverge}
\usage{
rescale_diverge(scale, center = 0)
}
\arguments{
\item{scale}{<\code{scale}> a scale object}

\item{center}{<\code{number}> the center of the scale input}
}
\description{
Creates a diverging scale with defined center / midpoint. Similar to \code{\link[=rescale_center]{rescale_center()}}, key difference is
the output palette / range is piecewise linear (with respect to breaks) and the entire output range is
always used.
}
\note{
Category and identity scales aren't supported.
}
\section{Centering vs Diverging}{
The plot below shows how \code{\link[=scale_center]{scale_center()}} and \code{\link[=scale_diverge]{scale_diverge()}} distort the scale output. The input
scale in this case is \code{power_scale(limits = -36:4)}, centered and diverged at 0 (which is 0.75 on the
original output).

\code{\link[=rescale_diverge]{rescale_diverge()}} is creating a piecewise scale, so the two halves of the output ramp have a different slope;
\code{\link[=rescale_center]{rescale_center()}} is keeping the output linear, but adjusting the slope such that y = 0.5 at x = 0.75 on
the linear ramp.

\figure{rescale.png}
}

\examples{
# create a diverging linear scale at 0
linear_diverged <- rescale_diverge(
  scale_color_linear(col, limits = -5:10),
  center = 0
)

# create a diverging log scale at 10
log_diverged <- rescale_diverge(
  scale_log(col, limits = 1:1000),
  center = 10
)
}
\seealso{
Other scales: 
\code{\link{rescale_center}()},
\code{\link{scale_category}()},
\code{\link{scale_identity}()},
\code{\link{scale_linear}()},
\code{\link{scale_log}()},
\code{\link{scale_power}()},
\code{\link{scale_quantile}()},
\code{\link{scale_quantize}()},
\code{\link{scale_symlog}()},
\code{\link{scale_threshold}()}
}
\concept{scales}
